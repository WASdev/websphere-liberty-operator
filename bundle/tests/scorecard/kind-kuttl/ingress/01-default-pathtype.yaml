apiVersion: liberty.websphere.ibm.com/v1
kind: WebSphereLibertyApplication
metadata:
  name: ingress
spec:
  license:
    accept: true
  route:
    host: myapp.mycompany.com
    path: "/"
    pathType:
