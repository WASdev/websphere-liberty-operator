---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: webspherelibertydumps.liberty.websphere.ibm.com
spec:
  group: liberty.websphere.ibm.com
  names:
    kind: WebSphereLibertyDump
    listKind: WebSphereLibertyDumpList
    plural: webspherelibertydumps
    shortNames:
    - wldump
    - wldumps
    singular: webspherelibertydump
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Indicates if dump operation has started
      jsonPath: .status.conditions[?(@.type=='Started')].status
      name: Started
      type: string
    - description: Reason for dump operation failing to start
      jsonPath: .status.conditions[?(@.type=='Started')].reason
      name: Reason
      priority: 1
      type: string
    - description: Message for dump operation failing to start
      jsonPath: .status.conditions[?(@.type=='Started')].message
      name: Message
      priority: 1
      type: string
    - description: Indicates if dump operation has completed
      jsonPath: .status.conditions[?(@.type=='Completed')].status
      name: Completed
      type: string
    - description: Reason for dump operation failing to complete
      jsonPath: .status.conditions[?(@.type=='Completed')].reason
      name: Reason
      priority: 1
      type: string
    - description: Message for dump operation failing to complete
      jsonPath: .status.conditions[?(@.type=='Completed')].message
      name: Message
      priority: 1
      type: string
    - description: Indicates filename of the server dump
      jsonPath: .status.dumpFile
      name: Dump file
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: 'Day-2 operation for generating server dumps. Documentation:
          For more information about installation parameters, see https://ibm.biz/wlo-crs.
          License: By installing this product, you accept the license terms at https://ibm.biz/was-license.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebSphereLibertyDumpSpec defines the desired state of WebSphereLibertyDump
            properties:
              include:
                description: 'Optional. List of memory dump types to request: thread,
                  heap, system.'
                items:
                  description: Defines the possible values for dump types
                  enum:
                  - thread
                  - heap
                  - system
                  type: string
                type: array
                x-kubernetes-list-type: set
              license:
                description: Optional. License information is no longer required for
                  the dump operation.
                properties:
                  accept:
                    description: Optional. License information is no longer required
                      for day-2 operations.
                    type: boolean
                type: object
              podName:
                description: The name of the Pod, which must be in the same namespace
                  as the WebSphereLibertyDump CR.
                type: string
            required:
            - podName
            type: object
          status:
            description: Defines the observed state of WebSphereLibertyDump
            properties:
              conditions:
                items:
                  description: OperationStatusCondition ...
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    lastUpdateTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      description: OperationStatusConditionType ...
                      type: string
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              dumpFile:
                description: Location of the generated dump file
                type: string
              observedGeneration:
                description: The generation identifier of this WebSphereLibertyDump
                  instance completely reconciled by the Operator.
                format: int64
                type: integer
              versions:
                properties:
                  reconciled:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
